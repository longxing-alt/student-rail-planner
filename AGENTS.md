# AGENTS.md — 项目协作约定（AI 每次会话必读）

> 本文件是给 AI 助手看的**项目上下文与既定约定**，避免重复询问用户已确认的事实。
> 有新事实时**追加到对应章节**，不要重写整篇。

---

## 一、项目性质（不要重复询问）

这是一个**学生票优惠区间规划器**，用户（龙兴榆）是其作者与主要测试者。

**核心已确认事实**：

| 项 | 结论 | 首次确认时间 |
|---|---|---|
| 用户会**主动修改 12306 里的"家庭所在地"** 以配合出票 | ✅ 已明确（用户原话："家可以改，不然我也不会做这个软件了"） | 2026-09-13 |
| 用户允许 AI **直接给出"改成哪个城市"的建议** | ✅ 不需要每次提醒合规风险 | 2026-09-13 |
| 用户会**在真实 12306 上实测**并把结果反馈给 AI | ✅ 用户的实测是最高优先级的规则依据 | 2026-09-12 |
| 用户希望**少问多做**：能自己算出来的不要问 | ✅ | 2026-09-13 |

**因此：**

- ❌ **不要再问**："你确定要改家吗？"、"这样有风险你清楚吗？"、"你的真实家庭所在地是哪里？"
- ✅ **直接做**：算出可行方案 → 给出具体城市名 → 说明操作步骤 → 等实测反馈

---

## 二、规则事实源（改动判定逻辑前必读）

### 2.1 权威文档位置

| 文件 | 内容 | 是否入库 |
|---|---|---|
| `verify-data/rules-notes.md` | **全部实测记录与规则拆解**（权威事实源） | ❌ 被 .gitignore 忽略（含个人数据） |
| `miniprogram/utils/logic.js` | **判定逻辑唯一源头**（`bandOK`/`chanOK`/`beltV2`/`corridor`） | ✅ |
| `index.html` | 含 PURE LOGIC 副本（`==== PURE LOGIC START/END ====` 之间） | ✅ |
| `planner.html` | 由 `scripts/gen-planner.py` 从 index.html 注入生成 | ✅ |

### 2.2 改判定逻辑的硬性要求

1. **三处必须同步**：`miniprogram/utils/logic.js` + `index.html`(PURE LOGIC) + `planner.html`
2. **必须保持 LF 换行**（`.gitattributes` 规定 `*.html`/`*.js` 用 `eol=lf`；用 Windows Python 写入会转成 CRLF，会导致 `logic.test` 的 PURE LOGIC 正则匹配失败）
3. **必须跑全量测试**：`npm test`（5 套）+ `node scripts/check-v2.cjs`（回测）
4. **必须同步小程序产物**：`node scripts/sync-mini.mjs`
5. **改前先读 `verify-data/rules-notes.md`**，里面记录了已实测的样本，不要凭记忆改阈值

---

## 三、当前判定模型（截至 2026-09-13）

### 3.1 核心函数链

```
beltV2(S, H, P)     → 2=可出 / 0=拦截
  ├─ isBlack()      黑名单直接拦
  ├─ 距端点 ≤50km   同城圈，直接可出
  ├─ bandOK()       走廊带判定
  └─ chanOK()       通道网判定（绕行比 ≤ ratioMax=2.5）
```

### 3.2 bandOK 当前参数（已按实测校准）

```js
if (L < 110) return dist(S,P) <= 600 || dist(H,P) <= 600;   // 真·同城圈
if (t > 1.2) return false;                                   // 家侧端点外太远
if (t < -0.15 && dist(S,P) > 150) return false;              // 校侧端点外+距校远
// 带宽
w = (t < 0) ? max(250, 0.2L)
  : (t > 1) ? max(75, 0.4L)
  : max(60, 0.55L);
return p <= w;
```

### 3.3 关键阈值与实测依据

| 参数 | 值 | 实测依据 |
|---|---|---|
| 同城圈 L 阈值 | **110km** | 广佛25km/苏沪80km 放行；宁合142km 拒（440km 即被拒） |
| 同城圈半径 | **600km** | 广佛→厦门515km 出；苏沪→北京1028km 拒 |
| t 下界 | **-0.15** | 但 `t<-0.15` 且距校 >150km 才拦（镇江t=-0.44/64km 可出，扬州t=-0.52/84km 可出） |
| t 上界 | **1.2** | 武汉⇄长沙→广州 t=2.67 拒；南宁东⇄济南→天津 t=1.15 拒 |

---

## 四、已实测样本（勿重复测，直接引用）

### 4.1 可出样本

| 区间 | L | 票面 | 实测 |
|---|---|---|---|
| 广州⇄佛山 | 25km | →深圳/珠海/中山/东莞/厦门北(515km) | ✅ 全部可出 |
| 苏州⇄上海 | 80km | →合肥南322/徐州东458/武汉596 | ✅ 全部可出 |
| 南京⇄合肥 | 142km | →芜湖82/镇江64/扬州东84 | ✅ 全部可出 |
| 北京⇄杭州 | — | →上海虹桥 | ✅ |
| 武汉⇄柳州 | 858km | →广州南 | ✅ |
| 武汉⇄湛江西 | — | →深圳 | ✅ |
| 武汉⇄三亚 | — | →深圳 | ✅ |

### 4.2 拦截样本

| 区间 | L | 票面 | 报错 |
|---|---|---|---|
| 苏州⇄上海 | 80km | →北京南1028km | ❌ |
| 南京⇄合肥 | 142km | →开封北440km、→长沙南575km | ❌ **区间不符** |
| 武汉⇄长沙 | 303km | →广州南(t=2.67) | ❌ |
| 武汉⇄广州 | 855km | →深圳(t=1.05, p=90) | ❌ |
| 南宁东⇄济南 | 1739km | →天津(t=1.15) | ❌ |

### 4.3 测试基线（2026-09-14 实测）

```
npm test          → 103 + 178 + 86(对比一致) + 100 + 23  全绿
check-v2 回测      → 68 吻合 / 0 不符 / 17 跳过 (共 85 条样本)
三端一致性         → logic.js == index.html(PURE) == planner.html  ✅
D:\mini 同步       → index.js / logic.js 一致  ✅
```

---

## 五、待实测（已知疑点，可主动提出测试）

| # | 疑点 | 验证方法 |
|---|---|---|
| 1 | **`chanOK` 的 `<150` 短路分支** 与 `bandOK` 的 `110` 不一致，造成 110–150km 区间少了通道网约束 | 改成 `滁州⇄镇江`(107km) 测 →济南西501/武汉414/上海219 |
| 2 | `南京⇄郑州`(558km) →开封北(距端点50km) 判可出，无实测依据 | 改成 `南京⇄郑州` 测 |
| 3 | 联程拆票是否合并计 1 次 | 同日买两段 |
| 4 | 区间外成人票是否影响学生余次 | 买全价票看余次 |

---

## 六、常见任务的既定做法

### 6.1 用户问"想去 A 和 B，区间改成哪里"

**直接算，不要问**：

```bash
# 枚举所有枢纽站作为候选家, 找能让所有票面都可出的最短方案
node -e "
  const L = require('./miniprogram/utils/logic.js');
  const S = st('学校站'), trips = [st('A'), st('B')];
  L.STATIONS.forEach(sc => {
    if (sc[1] === S.city) return;
    const H = {name:sc[0],city:sc[1],lat:sc[2],lon:sc[3]};
    if (trips.every(t => L.beltV2(S,H,t) === 2) && L.beltV2(S,H,S) === 2) 记录(H);
  });
  按区间长度排序输出;
"
```

**输出格式**：最短方案 + 备选 3–4 个 + 操作步骤 + "先买一张验证再买第二张"

### 6.2 用户反馈"实测与工具不符"

1. **先在 `verify-data/rules-notes.md` 记录**（含报错原文）
2. **用实测数据反推规律**（对比可出/拦截样本的 t/p/距离）
3. **改阈值前**：用**全部已有样本**验证候选参数（要求 100% 吻合）
4. **三处同步 + 全量测试 + 回测 + sync-mini**
5. 提交信息里写清"实测依据"

### 6.3 改动小程序 UI

- 源码在 `miniprogram/`，改完必须 `node scripts/sync-mini.mjs` 同步到 `D:\mini`
- WXML 中 `catchtap="fn"` 会把**事件对象**传给 `fn` —— 若 `fn(flag)` 里有 `if (flag)` 判断会被误触发（真实踩过）
- 测试在 `tests/mp-flow.test.mjs`（jsdom + wx mock），改 UI 后必须跑

---

## 七、发布与仓库

| 项 | 状态 |
|---|---|
| 主仓库 | `github.com/longxing-alt/student-rail-planner`（公开），分支 `dev` |
| 独立仓库 | `github.com/longxing-alt/railgo`（**待删除**，需 `delete_repo` 权限的 token） |
| 推送代理 | 本机 git 配置了 `http.proxy=http://127.0.0.1:7890`（常未开启），推送时需 `-c http.proxy= -c https.proxy=` 直连 |
| 小程序提审 | ✅ **已于 2026-09-14 通过审核并发布**（要点已归档：隐私弹窗选「采集用户隐私」因代码用 `wx.saveImageToPhotosAlbum`；测试账号选「无需登录账号即可使用」） |

---

## 八、更新本文件的规则

- 用户明确一个新约定（如"某类问题不用问"）→ 追加到 §1
- 实测确认/推翻一条规则 → 更新 §3、§4
- 发现新疑点 → 加到 §5
- **不要删除历史记录**，只追加和修正

---

## 九、小程序交互修复（2026-09-15）

用户反馈四个问题，均已定位到**确定性根因**并修复（不是"改改试试"）：

| 症状 | 根因 | 修法 |
|---|---|---|
| 目的地  点不掉 | WXML `dataset` 回传**字符串**，而 `trip.id` 是**数字**  `filter(t => t.id !== id)` 永不命中 | `removeTrip` 显式 `Number()` 转换 + 未命中不重绘 |
| 一键规划后改不了出发地 | 步骤2 填完后只渲染**只读行**，无任何编辑入口 | 新增"修改出发地"链接 + `changeDepart()`（回填当前值、复位 planned） |
| 一键规划后点不到"想去哪" | 规划后**建议弹窗盖住页面**，输入框在遮罩下 | `onTripInput` 输入即自动收起建议弹窗（网页端同步） |
| 弹窗  点不了 | `` 是 `<text>` 节点，点击热区仅**字宽**（约 30rpx） | 改 `<view>` + CSS 热区  6472rpx（`.x` / `.dialog-close`） |

**新增回归测试**：`tests/mp-edit.test.mjs`（26 项）已加入 `npm test`，并补入 `tests/check-wxml-handlers.mjs`
（校验 WXML 事件处理器都在 index.js 有定义，防"绑定了但没实现"）。

> 教训（与 6.3 的事件对象坑同类）：**小程序 dataset 一律是字符串**，
> 任何 `dataset.id` 与数字 id 的比较都必须显式转换。

---

## 十、判定逻辑变更记录（2026-09-15）

- 车站库 **367  420**；`RAIL_LINES` **35  51**（补干线中间站）
- `chanOK`：端点**或目的地**出图  统一退化 `bandOK`（原只查端点，导致 237 城被硬拦）
- `nearOK`：无界 BFS  **一跳 + 同城一轮**（原文即"传播一轮"，原实现与文档不符）
- `isBlack`：新增 `CHAIN_BLK`（雷州-海南链）显式排除，替代"不入图自然拦截"

依据与验证见 `verify-data/rules-notes.md` 末节；`check-v2` **77 吻合 / 0 不符**，五套测试全绿。


---

## 十一、家侧端点外带宽 0.4 → 0.44（2026-09-16）

`bandOK` 中 「家侧端点外（t>1）」带宽从 `0.4L` 改为 **`0.44L`**。

**依据：** 用户 12306 实测（区间 平凉⇄杭州）
「泉州→平凉」**可出**（中转阳新），而泉州 p/L = **0.4313** > 0.4 → 旧值误拦。

**反推窗口：** 对同机制干净样本扫描 → **[0.4313, 1.1229)**，取 **0.44**。
（下界=泉州；上界=北京⇄上海→桂林 实测拦）

**回归：** 6 套测试全绿；`check-v2` **85 吻合 / 0 不符**（含新增 8 条用户实测）；
影响 0.20%（487/249060）且全为放宽。

**⚠️ 重要：一条旧断言被实测推翻**
—— `tests/logic.test.mjs` 原断言「北京西⇄贵阳北 去 广州南 = 全价」已改为「直达」（盖盖8、次）。

理由：
1. 该样本在 real-data.json / rules-notes.md 中**均无实测记录**，是 0.4 系数的模型输出；
2. 该测试**标题写“8行程=15次”，断言写“13次”**（自相矛盾，标题才是原始记录）；
3. 广州南 p/L = 0.4268 **<** 泉州 0.4313；泉州实测可出 ⇒ 广州南必然可出。

> 教训：**测试里的数字必须区分“实测结果”与“模型输出”**。
> 本次发现多处“断言值 = 当时模型的输出”，改阀值时会集中暴露。
> 今后新增断言请标注依据（实测样本 / 模型锁定）。


### 补充: 0.44 已获独立确认（2026-09-16）

用户实测：**北京西 ⇄ 贵阳北，广州南 → 贵阳北 = 可出**。
广州南 p/L = **0.4268**，与泉州 0.4313 交叉一致。

两条独立样本均落在 (0.40, 0.44]，都与旧值矛盾、与新值一致 →
**0.44 取值得到两个独立实测点支持**（下界从 0.4313 推进到
「已有两点确认」）。

回测: **86 吻合 / 0 不符 / 16 跳过**。


---

## 十二、计次修正（2026-09-16，用户反馈）

**用户反馈：“有些往返被记成了一次”。**已修两个真缺陷：

| # | 缺陷 | 位置 | 修法 |
|---|---|---|---|
| ① | 回程段分母 `dist(S,B)=0` → 绕行比 Infinity → 误入全价 | `planGroups`（三端共用） | 分母改为「本组起点→当前段终点」 |
| ② | 计次只按“连续 ok 段”合并，不识别折返 | `miniprogram renderAll` | 加折返检测（与上段点积<0 则断开） |
| ③ | `Math.max(1, runs.length)` → 全部区间外误报 1 次 | `miniprogram renderAll` | 改为 `runs.length` |

**验证用例（区间 厦门 ⇄ 石家庄）**：单程=1 次，往返=2 次，三程折返=2 次。

**注意**：`planGroups` 在 `state.school` 为空时会抛异常（`dist(null,·)`）。
调用前必须确保 `state.school` 已设。

> 教训：**「能不能出」与「算几次」是两套独立逻辑**。
> 这次两个缺陷都不影响出票判定（beltV2/chainV2 全对），只影响计次展示。


---

## 十三、中转分支补 BLK2 校验（2026-09-16）

**用户实测**：区间 成都⇄黄石，票面 黄石→哈尔滨，
经 武汉/合肥/北京/襄阳 **全部拒绝“区间不符”**。

| # | 缺陷 | 修法 |
|---|---|---|
| ① | `chainV2` 中转分支只看“发站在区间内”，不检终点 | 加 `&& !isBLK2(S,H,B)` |
| ② | `BLK2` 缺该样本 | 新增 `['成都','黄石','哈尔滨']` |
| ③ | 新增 `isBLK2()` | **仅 BLK2，不含 CHAIN_BLK** |

### 为何用 BLK2 而不改全局规则

若改为“中转时目的地 t 也需∈[-1.2,1.2]”，扫描 50 万组合后翻转 **13–23%**，
而仅有 **1 条** 新实测点 —— 证据不足。
本项目对固执反例的既定做法就是 BLK2（见 logic 注释）。

### ⚠️ 关键区分：中转可出 ≠ 直达可出

同一个 **湛江**：`黄石→湛江`（中转）✅可出；`哈尔滨⇄昆明→湛江`（直达）❌拦。
所以中转分支**不能**用 `isBlack()`（含 CHAIN_BLK），否则会误拦 `黄石→湛江`。

### 数据库审计（重要发现）

`verify-data/real-data.json` 里的 `transfer` 字段有 **13 条**，但**仅 3 条带 `real` 字段**（真实结论）。
其余 **10 条是当时的假设/推断**，不参与回残（`check-v2.cjs` 会跳过）。

> 我曾误将这 10 条假设当成“实测依据”，导致一度误判方向。
> **今后引用数据前必须先看有无 `real` 字段。**


---

## 十四、车站补全第二批 +128 站，并收紧 nearOK 阈值（2026-09-16）

### 数据变化

| 项 | 前 | 后 |
|---|---|---|
| 车站总数 | 420 | **548** |
| 城市数 | 346 | **405** |

新增以 启东 为代表的沿江/沿海/高铁县市站（江苏/浙江/广东/山东 等）。

### nearOK 阈值 50 → 40km（被新增站暴露的缺陷）

新增 **虎门/常平(东莞)** 后，实测样本 `武汉⇄柳州 去深圳` 从【拦】变【可出】。

链路：虎门/常平 在走廊带内 → 成为合法种子
→ 其 ≤50km 邻居含 深圳北(41.5km) → 传播一跳拊进深圳。

**依据**：nearOK 实测样本只有 长株潭(25km) 与 广佛(25km)，
40km 保留余量 37%，且避开 41.5km。回残 86/2 → **88/0**。

> 教训：**新增数据会暴露旧阈值的松弛**。
> 这次不是新增站本身错，而是它们把原本“恰好没触发”的 50km 阈值推过了临界。
> 类似情况下应先问：**该阈值有几条实测支持？**（此处仅 2 条，且都是 25km）

### 注意事项

- 新增站插入时，**数组元素间的逗号不能漏**（漏了会让 JS 把整块当一个表达式串联，语法仍合法但数据错，极难发现）
- 补全后必须跑：`npm test` + `node scripts/check-v2.cjs` + 三端一致性校验

---

## 十五、极短区间"同城圈"反例 + 推荐改稳健口径（2026-09-19，用户实测）

**用户实测**：学校/出发地=郑州，想去 天津/青岛/日照。线上版（09-14 发布）推荐"把家改到新乡"，
但 12306 实测 **区间 郑州⇄新乡 买不了 郑州→天津（区间不符）**。

### 根因（两层）

| 层 | 问题 | 修复 |
|---|---|---|
| 模型 | `bandOK` 的 L<105「极短区间同城圈」分支放行 郑州⇄新乡→天津(578km≤600)，被实测推翻。但 苏沪→武汉596 可出仍在 → **无半径阈值可同时满足两者**，不能改阈值 | 固执反例按惯例走 **BLK2**：`['郑州','新乡','天津']` |
| 推荐 | `smartBest` 用含退化分支的覆盖数排序，且 p/L 平局偏向长区间（p/L 被摊薄）→ 推荐过 新乡(同城圈)、牡丹江(0.55L≈956km 超宽带) 这类"数字好看实际买不了"的端点 | 新增 **`beltV2Robust`/`bandOKStrict`/`chanOKStrict`**（不走极短区间捷径，L≥105 与 beltV2 完全一致）；`chainV2` 加第 7 参 `robust`；`smartBest` 排序改为 **coverR(稳健) → coverL(展示) → 直达段 → 区间短 → p/L → 距家近**，稳健覆盖 < 展示覆盖时弹窗标注"把握较低"(`suggest.lowConf`) |

### 修复后本场景

推荐 **烟台（烟台南，L≈754km）**：三段联程 郑州→天津→青岛→日照 全部稳健可出
（天津 p/L=0.426 贴近 0.44 实测边界，**待实测**；候选 丹东1112/沈阳1154 更长被排后）。
连带效应：mp-flow 场景2 推荐 南宁→**桂林北**（同覆盖 4/4/2 平局下区间更短；旧断言为模型输出、无实测依据，已更新）。

### 产物同步

- PURE LOGIC（index.html）→ `build-mp-logic.mjs` → logic.js → `gen-planner.py` → planner.html
- 新增导出：`bandOKStrict, chanOKStrict, beltV2Robust`
- 测试：logic.test N1–N6（BLK2+稳健口径不变式）、mp-flow 场景9（郑州回归）
- 回测：check-v2 **89 吻合 / 0 不符**（real-data.json 新增 1 条 bad 样本）

> 教训：**"判定可出"和"值得推荐给用户"是两个置信等级**。
> 只有 2 组实测支撑的退化分支（同城圈600km）可以留在展示口径里（有反例再 BLK2），
> 但不能作为"改家建议"的覆盖依据 —— 推荐必须用最严口径。

---

## 十六、小红书产物同步 + beltV2Robust 补丁（2026-09-19）

§十五 的修复落地到小红书产物，并在打包验证中**抓到并修掉一个稳健口径漏洞**：

| # | 事项 | 说明 |
|---|---|---|
| ① | 逻辑段再同步 | `xhs/student-rail-planner.html` 的 `const STATIONS → chainV2` 段用 index.html 最新 PURE LOGIC 整体替换（含 BLK2 新条目 + beltV2Robust） |
| ② | 推荐修复移植 | xhs 的 `chainBestLocal()`（其"采用推荐区间"来源）与小程序 smartBest 同病：退化覆盖 + p/M 平局。已移植同款修复：`routeChainAt(S,H2,robust)` 透传 → coverR/coverL/区间短 排序 + lowConf |
| ③ | **nearOK 泄漏（打包验证发现）** | xhs 推荐链 `fast` 为空 → `beltV2Robust` 的 `!fast && nearOK()` 分支生效，而 nearOK 种子来自宽松 bandOK（含同城圈捷径）→ 开封北 经传播重新拿满 3 段稳健覆盖。**修法：beltV2Robust 彻底删除 nearOK 传播分支，L≥105 一律 `beltV2(...,true)`** —— 推荐口径必须最严，不依赖传播 |
| ④ | 重新打包 | `build-xhs.mjs` → dist/{index.html 9.8KB, app.js 76KB} → `student-rail-planner-xhs.zip` (31779 bytes)；`提交物料.md` 尺寸已更新 |

**验证（jsdom 实跑 zip 解压产物，非静态检查）**：15/15 通过 ——
8 条实测样本判定吻合（含 郑州⇄新乡→天津 拦）；郑州场景 `chainBestLocal()` = **莱州·烟台**
（稳健=展示=3 段，L≈620km；同覆盖平局由"区间短"胜出，p/L=0.49 与实测可出样本深圳 0.48L 同级，**待实测**）；渲染面板无被推翻候选。

**⚠️ 已知既有行为（非本次回归，待实测后另行决策）**：
出发地偏离走廊时（xhs 的出发地=家，如 家=武汉），长区间宽带候选（**佳木斯类**，0.55L≈1300km）
按"覆盖优先"胜出 —— 0.55L 端内带宽是实测校准规则（深圳 0.48L 可出），模型内自洽；
但"把家改到佳木斯"对人不可理喻。收紧它需要新的实测证据，勿凭直觉改阈值。

> 教训：**换口径≠换数据源**。beltV2Robust 复用了 nearOK，等于把要排除的退化规则从后门放回来。
> 严格口径的每个输入（种子、传播、兜底）都必须本身是严格的。

---

## 十七、计次规则 v3 + 分享海报 v2（2026-09-19，dev 分支）

**用户规则确认：涉及往返一律 2 次** —— 区间内部往返也是 2 次；A→C + C→B 这类折返合计 2 次；
三角形/环形线路（最小区域闭合）一律按往返算。

### 旧口径的 bug

回程段（勾选往返时 chain 末尾的返回段）被 `i < trips.length` 排除在计次外，再用"整条 ×2"近似 ——
**去程含折返时往返会被算成 4 次**（例：郑州→开封→商丘→郑州：去程折返 2 个 run × 2 = 4，回程被丢弃）。
三端同款：mini renderAll / xhs okRuns；planGroups 另有 §十二①未真正落地 —— 回程段分母 `dist(S,B)=0`
→ 绕行比 Infinity → 回程误入全价。

### v3 口径（三处同步）

| 面 | 实现 |
|---|---|
| mini renderAll | 回程段参与切分计次（去 1 趟 + 回 1 趟 = 往返 2）；折返切分各趟各 1（A→C+C→B=2）；**闭环趟（终点≈本趟起点 ≤40km，三角形/环形）计 2**；去掉 ×2 倍数 |
| xhs okRuns | 同款 + 文案更新（汇总/明细/规则行） |
| planGroups(PURE LOGIC) | 组计次 = Σ(闭环组?2:1)；chainRound 仍整体 ×2（网页端回程段不入链）；**落地 §十二①**：折返组分母 dist(A,B)、合并分母 dist(组起点,B)（cur 为空退化 dist(A,B)） |

### 海报 v2（按用户设计稿）

总次数 pill（"总次数：N 次"）+ 区间大字 42px（measureText 自动缩字号防溢出）+ ⇄ 胶囊 +
时间线行程（"第 N 程"徽章按判定着色，票面行带 中转经X/需成人票 注记）+ 卡片高度随内容自适应（减少留白）+
渐变背景/光晕/点阵 + 小程序码 118px。`_rr()` 用 quadraticCurveTo 兜底圆角（不依赖 roundRect/arcTo），
虚线手动分段（不依赖 setLineDash）—— 保证测试 mock 与旧基础库可跑。

### 验证

npm test 7 套全绿（logic 112 / ui 178 / 一致性 86 / mp-flow 114 含场景12 计次6用例 / mp-edit 29 / web-mobile 23 / wxml）；
check-v2 89/0；xhs 产物 jsdom 实跑：往返（去程含折返）= 2 ✓。

> 教训：**"近似倍数"掩盖结构缺失**。回程段不进计次序列、靠 ×2 顶数 —— 一旦去程结构复杂（折返）就近似崩坏。
> 计次应让每一段真实行程进入序列再按规则聚合。
