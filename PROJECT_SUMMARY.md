# 学生优惠 · 区间规划器 —— 项目交接文档

> 给新会话的完整上下文。读取本文件即可理解项目现状并继续开发。

## 一、项目是什么

帮助中国大学生规划 **12306 学生优惠票**的单文件网页应用。核心回答三个问题：
1. **能去哪**（区间画图 + 区间内可去推荐）
2. **去一趟扣几次**（直达 1 次 / 中转 1 次 / 往返 2 次 / 全价 0 次）
3. **怎么改最省**（把"家"改到哪，枚举 367 站自动推荐）

- 线上地址：**https://longxing-alt.github.io/student-rail-planner/**（GitHub Pages，永久免费）
- 仓库：`longxing-alt/student-rail-planner`（本机 git remote 已配置）
- 本地开发：`python -m http.server 8624` → http://localhost:8624
- 单文件：`index.html`（约 127KB，零依赖，纯前端）

## 二、技术架构

- **单文件** `index.html`：CSS（自定义简约风）+ HTML + JS 全在一个文件
- **纯逻辑区**（`// ==== PURE LOGIC START ====` 到 `// ==== PURE LOGIC END ====` 标记内）：
  - `STATIONS`：367 个中国主要车站 `[名称,城市,纬度,经度,是否枢纽]`，35 个枢纽
  - `state`：全局状态（school/home/trips/ratio/budget/chainMode/chainEnd/touched 等）
  - `hav/dist`：Haversine 距离；`corridor`：点到"学校↔家"走廊的投影 t 与垂直偏差 p
  - `inIntervalBelt`：点在走廊带内判定
  - `directCovered`：直达判定
  - `transferPlan(S,H,O,D)`：中转方案（枢纽站 T，绕行 ≤ ratio×直达）
  - `planTrip(S,H,O,D)`：行程三态判定 → `direct / edge / transfer / full`
  - `evalWith(H)`：给定"家"评估全部行程；`evalAll()` 当前
  - `chainEval(S,H,stList,rStart,rEnd)`：串联路线判定（折返/绕行/区间内逐段；起点/终点可自定义，学校/家锚点自动通过；终点默认=最后一个地点）
  - `planOneWay`：兼容旧 3 参调用（出发地=学校）

## 三、核心判定规则（与 12306 实测校准，2026-08）

| 状态 | 条件 | 计次 |
| --- | --- | --- |
| ✅ 直达 | 出发地 O、目的地 D 都在核心走廊带（t∈[-0.05,1.05]，p≤max(60,0.32L)），且 D 在直达带（t∈[-0.12,1.12]，p≤max(45,0.18L)） | 1 次 |
| 🔀 中转 | 两端在走廊带 + 枢纽 T 在走廊带（t∈[-0.35,1.35]，p≤max(70,0.32L)）+ 绕行 ≤ ratio×直达（默认 1.5×） | 1 次 |
| 🟠 区间边缘 | 两端超出核心带但 ≤ 延伸带（t∈[-0.5,1.5]，p≤max(90,0.45L)）——实测可出票但可能被查 | 1 次（标风险） |
| ❌ 全价 | 任一端超出延伸带 | 0 次 |

- **往返 = 2 次**，单程 = 1 次（默认单程）
- **5 天联程规则**：整段行程出发日→结束日 ≤ 5 个日历天（按天数非小时），整条只扣 1 次
- **延伸带校准来源**：用户实测"济南以北最远沈阳（≈800km），天津/徐州可买，哈尔滨不行"——endExt = max(250, 0.45×L)
- **串联路线**：多个目的地按顺序串成一条联程（S→D1→…→终点），带折返检测（相邻段方向夹角>90°）与累计绕行 ≤ ratio 校验
- **出发地**：行程支持 `武汉到郑州` / `天津→济南` / `从天津到济南` 语法（无需"从"）；两端（出发+终点）都必须在区间内（实测"没有一端是学校不能用"）

## 四、UI/交互（按用户多轮反馈打磨）

- **向导式三步输入**（`wizard-bar` + `wz-panel`）：① 学校在哪 → ② 家在哪 → ③ 想去哪，每步只弹一个输入框，填完自动推进（`setGuideStep`/`renderWizard`），步骤条可回点
- **区间画图**（`intervalViz`）：🔒学校 — 家🔓 横线，已添加城市按 t 投影着色（🟢区间内/🟠边缘/🔴超区间）
- **区间内可去推荐**（`zoneCities`）：自动找走廊带内 6 个城市 + 2 个边缘城市，点一下即添加
- **4 步动画引导**（`spotlight` 聚焦框，sessionStorage 会话级记忆，每次打开播放）：①填学校 ②填家 ③加想去的地方 ④看区间线；聚焦框 clamp 视口内、面板切换同步
- **结果区动画**：仅点"🚀 规划"才自动上浮（`float-up`）+ 平滑滚动；改输入/添加地点自动下沉（`sink`）；`state.touched` 保证初始加载不自动滚动
- **配色**：清新蓝绿（主色 teal #0d9488、按钮青绿渐变 #06b6d4→#10b981、警示琥珀/红）
- **结果展示**：次数大数字 + 节点进度条（圆点嵌条，角色=起点/中转/终点）+ 摘要（"2 段学生票·1 段超区间"）+ 修改区间建议 + 先试买判断 + 拆开买最低统计 + 应用最优大弹窗（可下滑看详情）
- **其他**：地图可折叠（`toggleMap`）、高级设置折叠（预算/比例）、规则下拉菜单（导航）、步骤③地点列表拖拽排序（第一个=起点/最后=终点，不显示学校/家）、改终点（不去家）、批量往返/单程、应用最优按钮在串联控制条

## 五、测试体系

- `tests/logic.test.mjs`：纯逻辑 93 项（A距离/B走廊/C直达/D中转/E单程/F计次/G用户场景/H优化/I数据/J退化/K串联/L出发地）
- `tests/ui.test.mjs`：jsdom 端到端 161 项（T1初始/T2-3改学校家/T4-6增删行程/T7-8候选/T9视图/T10-11预算比例/T12快捷/T13防护/T14串联/T15下拉引导/T16出发地/T17拖拽终点5天/T18无案例区+试买提示/T19区间画图推荐/T20向导/T21引导对齐/T22-23仅规划才下滑/T24未完成三步隐藏/T25折返拆开买最低/T26优化器置顶+具体路线/T27"到"语法+清演示行程/T28顶部应用最优+弹窗/T29地点列表起点中转终点+拖拽+规划）
- 运行：`npm test`（= `node tests/logic.test.mjs && node tests/ui.test.mjs`，合计 254 项，全绿）
- jsdom 注意：文件 CRLF，patch 脚本需 `replace('\r\n','\n')`；`window.scrollTo`/`scrollIntoView` 在 jsdom 未实现（代码已 try/catch）；`state` 页面侧用 `w.eval()` 读取（测试 Node 侧 `state` 是另一 realm）

## 六、部署与推送

```bash
git add -A && git commit -m "描述" && git push   # GitHub Pages 1-2 分钟后自动更新
```
- **坑**：Git Credential Manager（GCM）弹窗常阻塞 push（`/dev/tty` 错误）——让用户确认桌面弹窗或重试；偶尔连续两次成功
- 验证线上：`curl -s https://longxing-alt.github.io/student-rail-planner/ | grep 特征词`
- 曾有 Netlify Drop 临时部署（已弃用，GitHub Pages 为准）

## 七、规则依据（小红书/官方，2026-08 收集）

- 官方：每学年 4 次单程（10.1–次年9.30）随时可用；家庭端可任意改不限次、学校端须与学信网一致不可动；5 天（120h 日历天）联程只扣 1 次；换乘站须在区间内；近径路/不故意绕远（150% 为实测阈值）；"能出票不代表合规"
- 实测规则（小红书/用户收集，2026-08）：超区间 780km 出票成功未查、完全不对应区间购票成功、两头非学校四次没查、被查风险高铁动车概率大、先试买判断法（12306 选票不付款看"区间不符"提示）等——已内化为走廊带/边缘带参数，不再在页面展示案例列表

## 八、已知局限与可继续方向

1. **几何走廊 vs 铁路径路**：走廊是直线近似，非铁路径路；边缘案例（如北京-贵阳区间把天津几何包含）可能误判——依赖"先试买判断"提示兜底
2. 车站坐标近似值；无实时班次/票价数据
3. 可选方向：接入真实铁路径路数据、票价估算（执行价 75 折）、分享导出、PWA 离线、多语言
4. 用户可能继续要求的：更细的区间可视化、行程对比（多个方案并列）、保存历史方案

## 九、快速启动

```bash
cd "C:\Users\龙兴榆\.zcode\workspace\default\student-rail-planner"
python -m http.server 8624        # 本地预览
node tests/logic.test.mjs         # 逻辑测试 93 项
node tests/ui.test.mjs            # UI 测试 161 项
```
