# 剩余待验证清单

> 你的中转实测已经暴露了一个**模型真实缺陷**，下面按优先级排列。

---

## 零、你的中转实测已发现的缺陷（最重要）

模型规则：**只有「枢纽站」或「区间端点」能当中转站**。

| 中转站 | 是否枢纽 | 你的实测 | 模型是否允许 |
|---|---|---|---|
| 汉口 | ❌ 否 | ✅ 可出 | ❌ 否 |
| 信阳东 | ❌ 否 | ✅ 可出 | ❌ 否 |
| 蚌埠南 | ❌ 否 | ✅ 可出 | ❌ 否 |
| 阳新 | ❌ 否（甚至未收录） | ✅ 可出 | ❌ 否 |
| 徐州东 | ✅ 是 | ❌ 不可 | ✅ 是 |

**实测成功的 4 个全部是非枢纽（模型会全部排除）；实测失败的那个是枢纽（模型会保留）。

⚠️ **注**：这是 **n=5** 的小样本，不足以推翻规则，但足以**标记为高优先疑点**。
另一种可能：徐州东失败与“中转站资格”无关，而是“该站无合适后续车次”。
需要更多样本才能定性。**

### 影响范围（已用全量组合核实）

我把「非枢纽也可中转」放开后扫了 **499800 组合**：

| 方案 | 结论翻转 |
|---|---|
| 放宽中转站候选池 | **0 条** |

**因为中转站只影响「怎么去」，不影响「能不能出」这个结论。**
（`chainV2` 的 hub 分支仅在「发站已在区间内、终点不在」时启用，只决定推荐哪个站。）

所以你的 泉州→平凉 **结论是对的**（可出），只是模型不会推荐“阳新”而已。

### 但确实存在的问题：推荐站质量

| 区间 | 票面 | 模型推荐经 | 实测可用 |
|---|---|---|---|
| 成都 ⇄ 黄石 | 黄石→哈尔滨 | 徐州东 | 襄阳 / 郑州 |
| 成都 ⇄ 黄石 | 黄石→拉萨 | 成都 | 郑州 / 西安 / 武汉 |

=> 「可中转」结论对，但**推荐的站不一定能买到**。

### 绕行比上限（另一个发现）

| 中转站 | 绕行比 | 实测 |
|---|---|---|
| 蚌埠南 | 1.17 | ✅ 可出 |
| 徐州东 | 1.23 | ❌ 不可 |

=> 真实上限 **∈ (1.17, 1.23]**，而模型用的是 **2.5**（偏松，也会影响推荐质量）。

---

## 一、建议优先验证：终点在区间外的中转建议

用你现在的区间（或任一你方便的），验证模型推荐的中转站**能不能真的买到学生票**。

> 这些都是已收录的实测样本（区间/票面已验过），只需验证“经该站中转”。

| # | 区间 | 票面 | 模型推荐经 | 验证点 |
|---|---|---|---|---|
| 1 | 滁州 ⇄ 镇江 | → 上海虹桥 | 合肥南 | 实测已知可出(经南京) —— 看模型推荐的合肥南能不能也行 |
| 2 | 滁州 ⇄ 蚌埠南 | → 徐州东 | 蚌埠南 | 实测已知可出 —— 验证上限 |
| 3 | 成都 ⇄ 黄石 | → 湛江 | 长沙南 | 实测已知中转可出(南昌/武汉) —— 看长沙南行不行 |
| 4 | 北京 ⇄ 广州 | → 湛江 | 郑州东 | 实测已知可出(广州作锚) —— 看郑州东行不行 |

---

## 二、我上次改动的 121 条「收紧」样本（一条都没验过）

这些是「旧判可出 → 新判拦截」。**若 12306 实际能出，就是我改错了。**

| # | 区间 | 票面 | 收紧原因 |
|---|---|---|---|
| 1 | 北京西 ⇄ 福州 | → 肇庆东 | 多跳传播泄漏 |
| 2 | 北京南 ⇄ 贵阳北 | → 珠海 | 多跳传播泄漏 |
| 3 | 石家庄 ⇄ 长沙南 | → 苏州 | 多跳传播泄漏 |
| 4 | 太原南 ⇄ 合肥南 | → 苏州 | 多跳传播泄漏 |

---

## 三、长区间宽中带（L≥2500, p>450）—— 13661 条，0 条实测

这一类目前依赖的是「0.55L 无上限」，且已知有一个反例
（海口⇄沈阳→徐州 实测拦，模型判可出）。

若你方便，随手测 2–3 条（区间任选一个长的，如 哈尔滨⇄拉萨）：

| 参考区间 | 票面 | p(km) | 容许宽度 |
|---|---|---|---|
| 哈尔滨西 ⇄ 拉萨 | → 南平 | 1391 | 1406 |
| 杭州东 ⇄ 乌鲁木齐 | → 龙岩 | 637 | 646 |

---

## 回复格式

```
滁州 ⇄ 镇江，上海虹桥 → 镇江：✅ 可出（中转合肥南）
北京西 ⇄ 福州，肇庆东 → 福州：❌ 区间不符
```

中转请标注**经哪个站**，以及那个站是否可行。
